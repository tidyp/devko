import React from "react";

const Tags = ({ tags }) => {
  console.log(tags)
  console.log(tags)
  console.log(tags)
  console.log(tags)
  console.log(tags)
  const tag = tags.split(",");
  return <div>{tag}</div>;
};

export default Tags;
